# StrikeCore toolbox proxychains config — routes rate-limited OSINT tools
# through the tor *container* (service name `tor` on strikecore-net).
strict_chain
proxy_dns
remote_dns_subnet 224
tcp_read_time_out 15000
tcp_connect_time_out 8000

[ProxyList]
socks5  tor  9050
